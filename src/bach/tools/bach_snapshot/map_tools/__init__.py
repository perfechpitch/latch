"""Map creation, editing, viewing, and topology helper tools.

This package owns user-facing map CRUD utilities. Runtime map phasing remains
in the root-level ``map_phaser.py`` because it is part of the simulator core
path used by ``top.py``.
"""
