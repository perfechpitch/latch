# 第 10 章　修订记录

**模式**：record（登记已经改过的口径，不定义规则；当前设计以各章正文为准）

一处口径改了，同一说法往往还散落在别处：图里的一行标注、接口清单的一句注释、另一份文档的取舍段落、模型代码的头注释。本章按修订项登记每次改动，一项一节，写清改前改后、依据、动过的位置，供三种情形查阅：

* 读到某处与记忆里不一样时，确认是改过还是漏改
* 补写新文档时，照最新那一档写，不照旧稿抄
* 同类说法再次出现时，照登记的范围扫一遍

《Bach 硬件设计建模参考》第 10 章，全套目录见 [README](README.md)。

***

## VU 与 DTE 的任务身份改为 dsa_ids 直连

**改动日期**：2026-09-08

**改前**：三个 DSA 的 `stream_id` / `task_id` / `user_id` 一律由软件写进各自的动态参数寄存器。RV core 从自定义 CSR 读出 TS 下发的这几个值，在写 Trigger 之前用配置指令写给 DSA。

**改后**：分成两条路。DTE 与 VU 各与本核那个 RV core 之间有一条 `dsa_ids` 直连，从 CSR 直接连过去、每拍有效，DSA 在写 Trigger 那一拍采样，软件不必再写一遍；DTE 取 `streamID` / `taskID` / `userID` / `pathID` 四项，VU 只取前两项。MU 没有这条线，那一组仍由软件写进它的动态配置寄存器。

**依据**：《VU-DSA 寄存器整理》的寄存器表里没有 `task_id`，`macro_inst_trigger` 的 `STREAM_ID_OVERRIDE` 描述为“`0` = 沿用 VU-Core CSR 中自带的 Stream ID”，即身份本来就在 CSR 侧，不在 DSA 的寄存器里。

**对软件的影响**：VU kernel 与 DTE kernel 不再需要写身份寄存器，MU kernel 照旧要写。`task_id` 在 VU 这一侧没有寄存器可配，`STREAM_ID_OVERRIDE` 只改 `stream_id`，改不了 `task_id`。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/vu.md` | 功能清单 F5、新增 F5a | `stream_id` 两个来源，`task_id` 只取 VU-Core CSR 那一份；那一组走 `dsa_ids` 直连 |
| 同上 | 第 0 层图 config_register 框 | 补一句身份来源；左侧补 `dsa_ids` 入口 |
| 同上 | 第 3 章接口 | 新增 `port dsa_ids`；`dsa_done` 的注释由“取自动态参数寄存器”改为“取自 dsa_ids 采样的那一组” |
| 同上 | 第 4 章存储器 `isq` | 表项补 `stream_id` 与 `task_id` 两项 |
| 同上 | 第 6 章 M1 图 | 补 `dsa_ids` 输入；第 3 步补同拍采样，选择逻辑从 M2 挪到这一级 |
| 同上 | 第 6 章 M2 图 | 第 4 步由“取自动态参数寄存器值”改为随描述符原样出队 |
| 同上 | 第 8 章机制覆盖 | `vu_ids_by_software` 拆成 `vu_ids_source` 与 `vu_ids_direct` |
| `07-units/chip/core/dte.md` | 第 3 章接口 | 新增 `port dsa_ids`，四个身份信号 |
| `07-units/chip/core/ts.md` | 功能清单 F82 | datain 完成事件的 `task_id` 由“软件写进 DSA 的配置寄存器”改为经 `dsa_ids` 直连到 DTE |
| `07-units/chip/core/rv-core.md` | 第 3 章接口 | `dsa_ids` 端口注释补各 DSA 的取法 |
| `software/软件产物与硬件配置.md` | 取舍段落 | 标题由“为什么身份字段由软件写而不是硬件直连”改为“为什么身份字段只有 MU 由软件写”，正文重写 |
| `src/bach/ip/chip/core/vu/retire.h` | 头注释 | `dsa_done` 的身份来源说明改为 `dsa_ids` 采样 |

**没有改的位置**：

* `07-units/chip/core/mu.md` F3、`src/bach/ip/chip/core/mu/regfile.h`、`src/bach/compiler/kernel/bach.h`：MU 确实由软件写，说法不变
* `03-core-内硬件.md` 的“CSR 与 dsa_iss 之间有双向连线”：这一句记的是原始 MAS 内部流水图上的连线，属于对原始文档的转述，不随建模口径改

***

## 用户号由两个改为一个

**改动日期**：2026-09-09

**改前**：用户号分成两个。`user_id` 16 bit，是 Router 与 credit 记账认的全局编号，建表时从 Router 请求里取；`local_user_id` 12 bit，是 core 内部软件自己编的号，只用于 R core 的用户映射表与 Matrix Mem 地址计算。两者互不相干，不存在换算关系，硬件不做任何转换。自启动的 B core 与 R core 上，软件认出用户后写回的那个值补进 `local_user_id`。

**改后**：只有 `user_id` 一个。RV core 的自定义 CSR 三个，`stream_id` 与 `task_id` 只读，`user_id` 可读写；它有两条写入路径，写的是同一个字段：普通计算 core 上 TS 下发 task 时硬件写入，B core 与 R core 上 TS 下发时还没有用户身份，软件在 `flag_check` 认出之后自己写，随 `task_done` 经 `rv_done` 回 TS 更新表项。软件读它算 R core 的用户映射表与 Matrix Mem 地址，Router 与 credit 记账认的也是它。

**依据**：RV Core MAS 的“自定义 CSR 寄存器”一节只列三个寄存器，`user_id` 那一条的原文是“用户在 HBU 流控范围内的 local user_id，有效位宽暂定为 12bit，可读写，传统业务流下由 TS 的 task 开始执行时从 task_queue 硬件写入 CSR（可以不写入），R-core 执行 flag_check 对应的 task 时，由软件 check 到 flag 对应的用户 user_id 后通过 CSR 写指令写入”，即同一个 CSR 的两条写入路径。同文档“task 完成通道”一节把回给 TS 的那一项叫 `local_user_id`，是同一个东西的另一个叫法。Task Scheduler MAS 的 stream_table 表项内容清单里也只有一个 `user_id`，自启动那一路写的是“等该自启动任务 (rv core) 返回 user_id 更新 stream_table”。

**为什么会发现**：B core 的搬出任务开 `TASK_CREDIT_EN` 之后，连着送进来的三笔 token 只发得出一笔。TS 向 Router 申请资源时带的是表项的 `user_id`，自启动这一路它一直是 0，Router 按“同一笔连着两拍出现在端口上”去重时就把三笔当成了一笔。

**对软件的影响**：kernel 的 `set_local_user_id()` 改名 `set_user_id()`，写的 MMIO 口从 `TC_LOCAL_USER_ID` 改成 `TC_USER_ID`；`local_user_id()` 并进 `user_id()`。R core 的 `task_rc_find` 与 B core 的 `task_bc_wait` 认出用户后调的是新名字，`task_dte_rc_send` 算落点读的也是 `user_id()`。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/rv-core.md` | 功能清单 F3 到 F6 | 下发信息由六字段改为五字段；F4 从“两个编号互不相干”改成“自定义 CSR 三个，`user_id` 有两条写入路径”；F5 与 F6 的字段名跟着改 |
| 同上 | 第 3 章接口 | `task_cmd` 与 `rv_done` 去掉 `local_user_id[11:0]`，`rv_done` 改带 `user_id[15:0]` |
| 同上 | 第 4 章存储器 | `task_q` 表项去掉一项；`csr` 的读写属性改为“`stream_id` 与 `task_id` 只读，`user_id` 可读写” |
| 同上 | 第 0 层图、第 6 章三张图 | CSR 标注、身份直连那一路、`rv_done` 的字段 |
| 同上 | 第 8 章机制覆盖 | `two_user_ids` 换成 `user_id_csr` |
| `07-units/chip/core/ts.md` | 功能清单 F14、F30、F86 | 自启动补进表项的字段名；用户级标记去掉一项；completion 口补写的字段名 |
| 同上 | 第 3 章接口、第 4 章 `stream_table` | 两处端口与表项清单各去掉一项 |
| 同上 | 第 6 章三张图、第 8 章机制覆盖 | 下发字段、完成字段、补写那一步；`two_user_ids` 换成 `one_user_id` |
| `03-core-内硬件.md` | RV core 的下发与完成字段表 | `local_user_id` 那一行改成 `user_id` |
| `07-units/chip/core/core.md` | 第 0 层图的连线说明 | `task_cmd` 与 `rv_done` 的字段 |
| `08-文档缺口与-TBD.md` | 口径冲突表 | 新增“用户号是一个还是两个”，结论已定 |
| `src/bach/ip/chip/core/ts/`、`rv_core/` | `StreamEntry`、`StreamWrite`、`TaskCmdPort`、`DonePort`、`TaskStartPort`、`RvTask`、`exec.h` 的 CSR 读写 | 两个字段合并成一个，`kCsrLocalUserId` 去掉 |
| `src/bach/compiler/kernel/bach.h` | task 控制区 | `TC_LOCAL_USER_ID` 去掉，`TC_USER_ID` 注释改为可读写；`set_local_user_id()` 改名 |

**没有改的位置**：

* `03-ts-任务调度器.md` 第 512 行“等自启动任务（RV core）返回 `user_id` 后再补进表项”：这一句本来就照 Task Scheduler MAS 的原话写，与新口径一致
* `03-core-内硬件.md` 的“CSR 与 dsa_iss 之间有双向连线（stream_id / user_id / task_id 随 DSA 指令下发）”：三个身份都在，说法不变
* 用户号的位宽：包头 16 bit、CSR 12 bit、`router_datain_notice` 10 bit 三处不一致，另记在第 8 章“用户号有几位”，等设计者确认

***

## DTE 中间 Buffer 的 8 KB 是每条通道一份

**改动日期**：2026-09-09

**改前**：inbound buffer 与 outbound buffer 合计约 8 KB，各 4 KB，按 256 B 一项算各 16 项。

**改后**：每条通道一份 8 KB，按 256 B 一项算 32 项。DTE 有五条物理数据通道（一条进核、四条出核），各一份。

**依据**：DTE MAS 的“主要参数介绍”表把 `Channel Buffer 8KB` 与 `物理数据 Channel 5`、`TaskQueue 16` 并列，一行一项，说的是每条通道的量；性能一节写“Ch 内 buffer：容量 8KB，最大可掩盖 32T latency”，32 拍乘 256 B 正好 8 KB。模型的一个 Lane 就是 MAS 的一条 Channel，里面含读写两侧，与“物理 Lane 10，每一个 Channel 存在两个 Lane”对得上。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/dte.md` | 功能清单 F24 | 由“两块合计约 8 KB”改为“每条通道一份 8 KB，32 项” |
| 同上 | 第 4 章存储器 | `in_buf` 与 `out_buf` 两行的容量与项数；`out_buf` 改成按通道索引 |
| 同上 | 第 0 层图两处标注、第 6 章两张图 | 容量标注 |
| 同上 | 第 7 章参数汇总 | “中间 Buffer”那一行 |
| `src/bach/ip/chip/core/dte/dte_types.h` | `kDteBufFlits` | 16 改成 32，注释重写 |

***

## CFG_DATA_LEN 按 8 B 一格

**改动日期**：2026-09-09

**改前**：文档没写这个寄存器的粒度，模型与 kernel 都按字节数配，两边自洽。

**改后**：`CFG_DATA_LEN` 是 16 bit，写进去的数以 8 B 为一格，硬件乘回字节，所以一段最长 64 KB 差 8 B，不足一格的尾巴配不出来。

**依据**：DTE MAS 的寄存器表里 `CFG_DATA_LEN0` 到 `CFG_DATA_LEN3` 四条的描述都是“段 N 有效传输长度，16 bit，粒度 8B”。

**对软件的影响**：kernel 的 `dte_move()` 写这个寄存器时除 `DTE_DATA_LEN_GRAIN`（8），传进去的字节数必须是 8 的倍数。现有的几处搬运长度都满足：token 512 B、FC1 与激活各 128 B、出核 reduce 包 24592 B、R core 一个槽 49664 B。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/dte.md` | 新增功能清单 F42a | 写明位宽与粒度 |
| `src/bach/ip/chip/core/dte/dte_types.h` | 新增 `kDteDataLenGrain` 与 `kDteDataLenMax` | 粒度与 16 bit 上限 |
| `src/bach/ip/chip/core/dte/regfile.h` | 写寄存器与生成描述符两处 | 写入时检查 16 bit 宽度，取出时乘 8 换回字节 |
| `src/bach/compiler/kernel/bach.h`、`kernel_dte.c` | `DTE_DATA_LEN_GRAIN`、`dte_move()` | 配寄存器时除 8 |

***

## VU 算力的精度口径改为 FP32

**改动日期**：2026-09-08

**改前**：VU 算力标注为“32 MAC/T（FP16 口径）”。

**改后**：改为“32 MAC/T（FP32 口径）”。数值不变，只改精度口径。

**依据**：同一份参数汇总里向量位宽写的是“1024 bit/cycle，等效每周期 32 个 FP32 或 64 个 BF16”，32 这个数按 FP32 算出来；VU 的内部计算精度只有 FP32 与 BF16 两种，没有 FP16。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/vu.md` | 第 7 章参数汇总 | “算力 32 MAC/T（FP16 口径）”的口径改为 FP32 |
| `05-建模参数与性能模型.md` | VU 算力那一行 | 表头“VU 算力（FP16）”改为“VU 算力（FP32）”，备注本来就写着 32 FP32 |

**没有改的位置**：

* `02-系统与部署.md` 算子表里 SiLU / dot 等的 `@FP16` 标注：那是算子输入输出的数据格式，不是 VU 算力口径。但 VU 的 CM 侧数据格式只有 FP8_e4m3 / MXFP8 / BF16 / FP32，这几处标注同样对不上，待确认后另行处理
* `05-建模参数与性能模型.md` 的“DTE 算力（FP16）”：DTE 不在本项范围内

***

## 装 weights 的包照常通知 TS，落点取自包头

**改动日期**：2026-09-09

**改前**：`op_type = 0` 记成“kernel / weight 搬运包进 core 时跳过 TS、直接唤醒 DTE”；进核那一笔的 `route` 按 core 的角色定死；weights 加载的数据路径写成“TS 派 DTE RV core 跑 weights loader → 算出 Matrix Mem 地址 → DTE 指令搬 Router → Matrix Mem”。

**改后**：跳过 TS 直接唤醒 DTE 的只有走 msg 流搬 kernel 那一档。装 weights 的包照常通知 TS，由 TS 派 datain 任务；数据由 DTE 按包头里的落点搬进 Matrix Mem，loader 数搬进来几笔，数够了中断 SCP。进核那一笔的 `route` 由 SCP 配：业务模式下按 core 的角色定，weights 加载阶段计算 core 上也落 Matrix Mem。

**依据**：《通信机制（分析过程）》与《MU-DTE 需求整理和遗留问题分析》两处写的都是“kernel 搬移 msg 进入 core 时，会跳过 TS，直接唤醒 DTE”，说的是 kernel 不是 weights，而走 msg 流搬 kernel 本身仍是待定需求；《系统软件需求分析》的 weights 加载步骤第一条就是“router 通知 ts，trigger datain 任务执行”。落点取自包头这一条与 R core 进核那一笔同源，依据是 DTE MAS 的 Header Parser 解析入站包头即生成 Descriptor、搬运随即开始。

**改到的位置**：

| 文件 | 位置 | 改了什么 |
| - | - | - |
| `07-units/chip/core/router.md` | F56、建模映射表 | `op_type = 0` 的说明改成走 msg 流搬 kernel 那一档，补上装 weights 照常通知 TS |
| `07-units/chip/core/router.md` | 新增 F54a、建模映射表 | 开了 `path_core_mask` 的 path 走到既不进核又没有下游的那一跳就终止 |
| `07-units/chip/core/dte.md` | F3c | `route` 从“按 core 的角色定死”改成“由 SCP 配”，补上 weights 阶段计算 core 也落 Matrix Mem |
| `02-scp-工作流程.md` | 装 weights 的数据路径、三处配置表的 DTE 行、切业务那一步 | 落点取自包头、loader 只计数；DTE 的进核落点两个阶段各配一次 |
| `08-文档缺口与-TBD.md` | “R core 进核那一笔的目的地址由谁给” | 扩成“进核那一笔的目的地址由谁给”，把 weights loader 算地址那一处并进同一条 |
| `software/软件产物与硬件配置.md` | 硬件不检查那一节 | 加“weights 加载阶段发方发几笔、收方数几笔两边说好” |
| `src/bach/ip/chip/core/dte/dte.h`、`core/core.h` | `SetInbound`、`SetWeightsInbound` / `SetBusinessInbound` | 进核那一笔的落点与回不回 Ack 从构造期定死改成能重配 |
| `src/bach/ip/chip/core/router/router_station.h` | `IssueRequest`、新增 `Drop` | 开了 `path_core_mask` 的表项走到头时把这一笔吃掉，不再断言 |
| `src/bach/compiler/kernel/kernel_dte.c`、`bach.h` | `task_dte_weights_loader`、`WEIGHTS_CNT_OFF` | loader 改成只数搬进来几笔 |
